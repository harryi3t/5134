language: none

build:
  ci:
    - echo "Hello, World!"
    - echo "This is master"

integrations:
  notifications:
    - integrationName: trigger_resource
      type: webhook
      payload:
        - resourceId=30
      branches:
        only:
          - master
      on_success: always
      on_failure: never
      on_start: always
