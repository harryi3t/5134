language: python

python:
  - 3.2

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE
    - M2=TWO

build:
  pre_ci_boot:
    image_name: shippable/minv2
    image_tag: latest
  
  ci:
    - echo "Hello, World!"
    
