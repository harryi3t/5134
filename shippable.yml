# Language setting
language: node_js

# Version number
node_js:
  - 0.10
  - 0.11

services:
  - elasticsearch

build:
  pre_ci_boot:
    image_name: shippable/minv2
    image_tag: latest
    pull: true
    options: '--privileged=true --net=bridge -e FOO=true -e BOO=false'
    
  ci:
    
    - echo "Hello, World!"
  
