# Language setting
language: node_js

# Version number
node_js:
  - 0.10
  - 0.11

services:
  - elasticsearch

build:
  pre_ci_boot:
    image_name: shippable/minv2
    image_tag: latest
    
  ci:
    
    - echo "Hello, World!"
  
