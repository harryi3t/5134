'use strict';

function hello (names){
  if(names.length > 0)
  names.forEach(
    function (name) {
      name[0] =name[0].toUpperCase();
      console.log(name)
    }
  );
}

function hello (names){
  if(names.length > 0)
  names.forEach(
    function (name) {
      name[0] =name[0].toUpperCase();
      console.log(name)
    }
  );
}

function hello (names){
  if(names.length > 0)
  names.forEach(function (name) {
    name[0] =name[0].toUpperCase();
    console.log(name)
  });
}

function hello (names){
  if(names.length > 0)
  names.forEach(
    function (name) {
      name[0] =name[0].toUpperCase();
      console.log(name)
    }
  );
}

function hello (names){
  if(names.length > 0)
  names.forEach(
    function (name) {
      name[0] =name[0].toUpperCase();
      console.log(name)
    }
  );
}

module.exports=hello;
